/*import axios from "axios";

const quizz = (store) => (next) => (action) => {
    switch (action.type) {
      case: {
        axios.get()
          .then((response) => {
           
          })
          .catch((error) => {
            console.log(error);
          });
  
        next(action);
        break;
      }
    };*/